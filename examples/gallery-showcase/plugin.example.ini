; ---------------------------------------------------------------------------
; fancy-gallery-showcase plugin configuration
;
; Copy these lines into your mumble-server.ini, then restart the
; server.
; ---------------------------------------------------------------------------

plugin.fancy-gallery-showcase.enabled=true
