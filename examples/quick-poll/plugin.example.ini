# Example INI block for the fancy-quick-poll plugin.
#
# Drop into your `mumble-server.ini` (or a file under `plugins.d/`)
# to enable the plugin.  The plugin reads no settings itself - this
# block exists only so the plugin shows up in the loader.

[plugin.fancy-quick-poll]
